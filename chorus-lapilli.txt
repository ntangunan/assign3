4/23/26
11:47 AM: Understanding the rules of the Chorus Lapilli game. Planning how to approach changing the logic of the current tic-tac-toe game, as well as thinking about what new features I need to implement to match the new game's gameplay.
    Observations:
        1. Each player takes turns placing their "X"/"O" on the board.
            a. "X"s and "O"s stop being placed after their each player has had their turn 3 times. 
            b. A win condition can be met during the placements phase.
        2. After Player 2 has placed their last O, Player 1 must move their X to an empty Square.
            a. Each player takes turns moving their "X"/"O" to an empty Square until the win condition is met.
            b. A player cannot move its "X"/"O" if there are no free empty Squares directly to its left, right, top, and bottom.
                i. A visual indication to the user should be displayed in order to let the user know what the valid moves are.
        3. The win condition is met if a player is able to move their "X"/"O" so that three of their "X"/"O" is matched three in a row across the board.
            a. The win condition is the same as in tic-tac-toe (3 across in any row, 3 across in any column, 3 across in any diagonal).

12:21 PM: First starting with the placement phase.
    Action: Added a counters using state variables to keep track of X and O turns. Implemented logic within already existing placement code to increment the counts. Included return early checks so that there exists 3 turns per player max.

    Result: Players are still able to win during this placement phase.

1:08 PM: Now I am looking into how to develop the logic for moving pieces after the placement phase.
    Action: Developed logic that detects if a valid square (correct X/O for current turn) has been selected after the placement phase has ended. Utilized a new state variable to validate if a square is currently selected.

1:45 PM: I now want to implement the move logic for valid moves a player can make after selecting a square.

    1. Implemented Piece Movement Phase and Selection Logic.
    
    2. Refactored handleClick to distinguish between the "Placement Phase" (first 3 moves per player) and the "Movement Phase" (after 6 total pieces are on the board).

    Observation: In Chorus Lapilli, once a player has 3 pieces, they cannot add more. They must select an existing piece and move it to an empty adjacent slot.

    Created an adjacentSquares helper function containing a lookup table for all 3x3 grid adjacencies (up, down, left, right, and diagonals).

    Modified handleClick to handle two distinct clicks during movement:

        - The first click selects the player's own piece.

        - The second click validates if the target square is adjacent and empty via the adjacentSquares filter.

    Current State: Players can now pick their X/O and place it in a valid spot. The piece count (xCount/oCount) remains constant at 3 per player once the movement phase begins. However, my code does not currently place the X/O on the board visually.

3:16 PM: Fixed bug where square chosen for piece to move does not display X/O piece. 
    Problem: Attempted to create a piece value to get from selectSquare in order to store its value in desired move square.

    Fix: This method was too overcomplicated, I could instead use prior logic used in order to visually input that piece. Recreated the nextSquares variable and made it local to the "Select square to move piece to" phase and made it choose X or O based on the value of xIsNext.

    Bug: Game still continues after a player has won.

4:23 PM: The last goal is to get the valid moves to be shown to the user after selecting a piece.
    Action: created a visualValidMoves state variable in order to keep track send the state to the Board compenent when rendered. Updated Square and Board components to handle the highlighting of squares based on the visualValidMoves state.

4:43 PM: Fixed bug in which players could move pieces after a player has won. An early return was used by calculating the winner with calculateWinner(squares) before anything else.
    Problem: Forgot to enforce rule that if a player placed a piece in the venter square (4) during the placement phase, when it is their turn they must either move that center piece or make a move that wins the game.

5:24 PM: Implemented special Center Rule by adding logic inside Movement Phase code. 
    Observation: The game requires that if a player occupies the center, their next move must either result in an immediate win or they must move the piece currently occupying the center square.
    
    Action: Implemented move smulation which creates a nextSquares copy to simulate the board state after a potential move but before updating the state.

    Updated Win Detection: Used calculateWinner(nextSquares) to determine if the proposed move ends the game.

    Rule Enforcement: Added a conditional check: if the player owns the center (squares[4]) and the proposed move does not result in a win (!wouldWin), the move is only valid if the piece being moved is the one in the center (selectSquare === 4).

    Wrap up: The game now fully follows the Chorus Lapilli rules. Players are blocked from making "non-winning" moves that leave their piece stuck in the center, and the game correctly identifies winners during the movement phase.